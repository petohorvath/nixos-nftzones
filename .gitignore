# nix build output symlinks
result
result-*

# editor autosave/backup files
*~
\#*\#
.\#*

# nix-direnv cache and per-user envrc overrides
.direnv/
.envrc.local

# git-hooks.nix writes this on `nix develop` / direnv reload;
# it's a build artifact, not source.
.pre-commit-config.yaml

# Claude Code writes a scheduled-task lock here; runtime state,
# not source.
.claude/
